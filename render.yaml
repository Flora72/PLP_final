services:
  - type: web
    name: healingatlas
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: python manage.py migrate && gunicorn atlas_core.wsgi:application
    staticPublishPath: staticfiles
